<%= node[:rails][:environment] %>:
  repository_base_path: <%= @storage %>
  repository_archive_path: <%= File.join( @storage, 'archive' ) %>
  svn_app_config:
    host: localhost
    port: 8001
  git_app_config:
    git_path: /usr/bin/git
  marker:
    link_base: /
